@model IEnumerable<TL_LMS.Models.Student>

@{
   // Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.student_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.s_department)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.s_grade)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.student_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.s_department)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.s_grade)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.student_reg_no }) |
                @Html.ActionLink("Details", "Details", new { id=item.student_reg_no }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.student_reg_no })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
